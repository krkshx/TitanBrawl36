#pragma once

// LatencyTestConfiguration — not yet reversed; encode/decode throw pending_reverse.
// Replaces the TITAN_PENDING_ENTRY(LatencyTestConfiguration) placeholder.

#include "titan/messages/Nested.hpp"

namespace titan {

TITAN_PENDING_ENTRY(LatencyTestConfiguration);

} // namespace titan
