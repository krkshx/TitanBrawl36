#pragma once

// TimedOffer — not yet reversed; encode/decode throw pending_reverse.
// Replaces the TITAN_PENDING_ENTRY(TimedOffer) placeholder.

#include "titan/messages/Nested.hpp"

namespace titan {

TITAN_PENDING_ENTRY(TimedOffer);

} // namespace titan
