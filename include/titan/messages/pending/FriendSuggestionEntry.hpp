#pragma once

// FriendSuggestionEntry — not yet reversed; encode/decode throw pending_reverse.
// Replaces the TITAN_PENDING_ENTRY(FriendSuggestionEntry) placeholder.

#include "titan/messages/Nested.hpp"

namespace titan {

TITAN_PENDING_ENTRY(FriendSuggestionEntry);

} // namespace titan
