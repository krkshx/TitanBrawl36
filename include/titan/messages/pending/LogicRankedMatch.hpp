#pragma once

// LogicRankedMatch — not yet reversed; encode/decode throw pending_reverse.
// Replaces the TITAN_PENDING_ENTRY(LogicRankedMatch) placeholder.

#include "titan/messages/Nested.hpp"

namespace titan {

TITAN_PENDING_ENTRY(LogicRankedMatch);

} // namespace titan
