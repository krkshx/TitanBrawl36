#pragma once

// AnalyticEvent — not yet reversed; encode/decode throw pending_reverse.
// Replaces the TITAN_PENDING_ENTRY(AnalyticEvent) placeholder.

#include "titan/messages/Nested.hpp"

namespace titan {

TITAN_PENDING_ENTRY(AnalyticEvent);

} // namespace titan
