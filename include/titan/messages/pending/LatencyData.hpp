#pragma once

// LatencyData — not yet reversed; encode/decode throw pending_reverse.
// Replaces the TITAN_PENDING_ENTRY(LatencyData) placeholder.

#include "titan/messages/Nested.hpp"

namespace titan {

TITAN_PENDING_ENTRY(LatencyData);

} // namespace titan
